# Mangarr runtime dependencies — exact pins.
#
# Dependency updates are reviewed manually while hosted automation is disabled.
# Reproduce locally with:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Keep each manual package update in a focused PR. Run `make test` and
# `make security-deps` before merging so regressions remain easy to bisect.

# Web framework + ASGI server
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.51.0

# Async HTTP client (used by SSRF guard, indexer adapters, etc.)
httpx==0.28.1
anyio==4.13.0

# Templating
jinja2==3.1.6

# Cover validation and format normalization
Pillow==12.3.0

# multipart/form-data parser — required for any FastAPI Form() endpoint
python-multipart==0.0.32

# CBR (RAR-archive) extraction in the import pipeline
rarfile==4.2

# Defused XML parsing for ComicInfo.xml — blocks XXE / billion-laughs
defusedxml==0.7.1

# Fernet-based encryption for at-rest secrets in the settings table
cryptography==48.0.1

# Argon2id password hashing for the local administrator account
argon2-cffi==25.1.0
